# Instagram_profile_details_using_python
using multiple python libraries, i did this project. it can does give details of a instagrm user, number of posts , number of followers , his/her following, along with profile url.

Instgram profile information

we can install required packages through terminal or cmd of requirements text file.
using this command:
pip install -r requirements.txt
we can Execute Program by this command:
python InstgramProfile.py <username>
